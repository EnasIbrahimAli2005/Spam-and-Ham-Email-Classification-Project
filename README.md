# Spam-and-Ham-Email-Classification-Project
Developed a machine learning model to classify emails as spam or ham (non-spam) using the 'spam.csv'  dataset. Applied text preprocessing techniques, including cleaning, tokenization, and TF-IDF feature  extraction, followed by classification models to achieve accurate detection of spam emails
